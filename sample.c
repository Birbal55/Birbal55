#include<iostream>
using namespace std;
int main()
{
	int ino,r;
	int coont;
	for
	(ino=1;ino<=100000;ino++)
	{
		r=1;
		count=0;
		while(r<=ino)
		{
			if(ino%r==0)
			   count++;
			r++;
			   
		}
		if(counnt==2)
		printf("/%d",ino);
		
	}
}
